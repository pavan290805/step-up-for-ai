export default function StudentDashboard() {
  return (
    <main className="module-placeholder">
      <section className="module-placeholder__panel">
        <h1>Student</h1>
        <p>Student module route is ready inside the unified React app.</p>
      </section>
    </main>
  );
}
